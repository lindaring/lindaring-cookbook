--SQL SERVER
CREATE TABLE activity (
    [id] int,
    [text] varchar(255),
    [dateAdded] datetime,
    [user] varchar(50)
);

--MYSQL
CREATE TABLE activity (
    `id` int,
    `text` varchar(255),
    `dateAdded` datetime,
    `user` varchar(50)
);